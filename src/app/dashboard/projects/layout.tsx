// company-website/src/app/dashboard/projects/layout.tsx
import React, { ReactNode } from 'react';

export default function ProjectsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}