// company-website/src/app/dashboard/testimonials/layout.tsx
import React, { ReactNode } from 'react';

export default function TestimonialsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}