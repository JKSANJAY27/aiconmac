// company-website/src/app/dashboard/contacts/layout.tsx
import React, { ReactNode } from 'react';

export default function ContactsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}